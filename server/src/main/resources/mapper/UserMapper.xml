<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zh.server.mapper.UserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zh.server.entity.User">
        <id column="id" property="tId" />
        <result column="name" property="tName" />
        <result column="phone" property="tPhone" />
<!--        <result column="telephone" property="telephone" />-->
        <result column="address" property="tAddress" />
        <result column="enabled" property="tEnable" />
        <result column="username" property="tUsername" />
        <result column="password" property="tPassword" />
        <result column="userFace" property="tUserFace" />
        <result column="remark" property="tRemark" />
    </resultMap>
    <resultMap id="AdminWithRole" type="com.zh.server.entity.User" extends="BaseResultMap">
        <collection property="roles" ofType="com.zh.server.entity.Role">
            <id column="rid" property="id" />
            <result column="rname" property="name" />
            <result column="rnameZh" property="nameZh" />
        </collection>
    </resultMap>


    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, phone, telephone, address, enabled, username, password, userFace, remark
    </sql>

    <!--获取所有操作员-->
    <select id="getAllAdmins" resultMap="AdminWithRole">
        select
        a.id,
        a.enabled,
        a.name,
        a.address,
        a.phone,
        a.remark,
        a.telephone,
        a.userFace,
        a.username,
        r.id as rid,
        r.name as rname,
        r.nameZh as rnameZh
        from
        t_admin as a
        LEFT JOIN t_admin_role as ar on a.id = ar.adminId
        LEFT JOIN t_role as r on ar.rid = r.id
        where a.id != #{id}
        <if test="null != keywords and '' != keywords">
            and a.name like concat('%',#{keywords},'%')
        </if>
        order by
        a.id
    </select>

</mapper>
